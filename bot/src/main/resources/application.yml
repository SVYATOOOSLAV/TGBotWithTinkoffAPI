app:
  telegram-token: ${BOT_TOKEN}
  tinkoff-token: ${TINKOFF_TOKEN}

spring:
  application:
    name: bot
  jackson:
    time-zone: UTC

server:
  port: 8090

logging:
  config: classpath:log4j2-plain.xml
